# Build

```
make massdrop/ctrl:default
```

 * If you use ctrl:mac, it will swap alt and cmd.
 * If you use ctrl:*_md, it will use the old MASSDROP animations and not the QMK ones.

# Deploy

```
./mdloader_mac --first --restart -D massdrop_ctrl_default.bin
Keyboard: Hold fn+b
```

